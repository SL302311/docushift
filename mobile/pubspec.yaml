name: docushift_mobile
description: "DocuShift — 本地文档格式转换移动端 (Android先行)"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.26.0

flutter:
  uses-material-design: true
