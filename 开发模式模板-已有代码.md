# 基于已有代码开发的分期协作模板

适用场景：已有项目、接手旧代码、README 混乱、方向不清，或者已经写了一部分但需要重新建立节奏。

核心模式：**先验真，再规划；先精简文档，再继续开发**。

不要一上来大改代码。先让架构师/检查者 agent 读取代码和文档，建立“当前真实状态”，再规划下一期。

## 角色分工

| 角色 | 职责 |
|---|---|
| 用户 / 产品负责人 | 说明项目最终方向，确认哪些旧功能保留、哪些可以放弃 |
| 架构师 / 检查者 agent | 读代码、验收、找风险、精简 README、规划下一期 |
| 实施 agent | 按 README 的下一期方案开发，不随意扩大范围 |

## 第 0 步：迁移与确认工作区

先确认：

- 项目真实路径。
- 当前主分支或当前代码状态。
- 是否有未提交或用户手改内容。
- 哪些目录是源码，哪些是构建产物。
- 最终产品方向是否改变。

建议开场提示：

```text
工作空间路径：
最终产品形态：
当前我希望你只验收/规划，不写代码。
```

## 第 1 步：先读代码，不写代码

架构师 agent 先读：

- README。
- 项目结构。
- package / requirements / 构建脚本。
- 前端入口。
- 后端入口。
- 数据模型。
- API。
- 测试。
- 截图。
- 发布产物。

优先命令：

```bash
rg --files
rg -n "^#|^##|^###" README.md
rg -n "@app\\.|router\\.|describe\\(|test_" .
```

## 第 2 步：建立当前真实状态

不要默认相信旧 README。以代码和可运行结果为准。

输出应该包括：

- 当前技术栈。
- 当前能做什么。
- 当前数据模型。
- 当前 API。
- 当前构建方式。
- 当前测试方式。
- 当前发布形态。
- 当前截图或运行证据。
- 当前高风险问题。

## 第 3 步：检查清单

| 项 | 检查问题 |
|---|---|
| 启动 | 项目能不能启动 |
| 构建 | 构建命令能不能过 |
| 测试 | 自动测试能不能跑 |
| 文档 | README 是否真实 |
| 数据 | 数据模型是否合理 |
| API | API 是否和前端一致 |
| 业务 | 核心业务语义是否正确 |
| UI | 是否符合产品形态 |
| 产物 | 是否有截图、安装包、部署或运行证据 |
| 路线 | 下一期是否足够小、明确、可验收 |

## 第 4 步：精简 README

已有项目最容易出现的问题是 README 变成流水账。

保留：

- 当前真实状态。
- 当前技术栈。
- 当前项目结构。
- 当前功能。
- 当前数据与业务规则。
- 当前问题。
- 下一期计划。
- 构建 / 测试 / 运行命令。

删除或压缩：

- 过期方案。
- 重复技术栈。
- 重复 API 表。
- 每期重复截图规划。
- 已完成但无长期价值的过程记录。
- 和代码不一致的描述。

精简后的 README 建议结构：

```md
# 项目名称

当前版本：
当前状态：
主交付形态：

## 当前定位

## 技术栈

## 项目结构

## 快速使用

## 构建与验证

## 当前功能

## API 概览

## 数据与业务规则

## 设计规范

## 截图

## 阶段路线

## 当前复核结论

## 下一期方案

## 给后续 agent 的工作原则
```

## 第 5 步：规划下一期

不要上来大重构。下一期应尽量小而关键。

优先级建议：

1. 修会误导用户的数据/业务语义。
2. 修会导致启动/构建/打包失败的问题。
3. 补测试。
4. 补缺失的核心用户流程。
5. 再做 UI 和体验。
6. 最后做新功能。

下一期方案必须包含：

- 目标。
- 技术方案。
- 项目结构变化。
- 实施顺序。
- 验收标准。
- 不做什么。

## 第 6 步：交给实施 agent

给实施 agent 的提示词：

```text
请阅读 README，按“下一期方案”实现。
要求：
- 不要扩大范围
- 不要重写无关模块
- 保留现有可用功能
- 优先修 README 中列出的 P1/P2 问题
- 补测试和构建验证
- 完成后更新 README 的完成情况
```

## 第 7 步：循环验收

每期结束后：

```text
实施完成
→ 架构师验收
→ 找问题
→ 精简 README
→ 规划下一期
→ 再交给实施 agent
```

## 复核输出模板

```md
## 第 N 期复核结论

结论：通过 / 基本通过 / 不通过。

### 已完成

| 项 | 结论 |
|---|---|

### 发现的问题

| 优先级 | 问题 | 影响 | 建议 |
|---|---|---|---|

### 验证结果

| 命令 | 结果 |
|---|---|

### 第 N+1 期方案

目标：

实施顺序：

验收标准：

不做：
```

## 关键原则

- 先读代码，再判断。
- README 必须代表当前真实状态。
- 不要让 README 持续累积重复历史。
- 架构师 agent 控制方向，实施 agent 负责执行。
- 每期只解决一个主问题。
- 业务语义优先于 UI，UI 优先于高级功能。
- 如果最终形态变了，例如从 HTML 改成 EXE 或手机 App，要立刻修订路线。

